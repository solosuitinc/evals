inbound:
  id: inbound.dev.v0
  description: Eval that verifies that the model can classify and pull data from inbound.
  metrics: [accuracy]

inbound.dev.v0:
  class: evals.elsuite.basic.match:Match
  args:
    samples_jsonl: inbound/samples.jsonl
